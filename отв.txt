Какой из предложенных методов для работы с RDD является действием? - Count 

Что будет выведено в результате выполнения данного запроса?
val movies = Seq(("Damon, Matt", "The Bourne Ultimatum", 2007L), ("Damon, Matt", "Good Will Hunting", 1997L))
val moviesDF = movies.toDF("actor", "title", "year").show
- Создается DataFram с тремя столбцами("actor", "title", "year") и двумя строками


За что отвечает метод take(n)? - получение списка, состоящего из n элементов

Преобразование select производит с DataFrame следующее. - Выбирает один или несколько столбцов из существующего набора столбцов в DataFrame

За что отвечает следующий фрагмент кода:
my_text_RDD = sc.textFile(‘file.csv’)
- создание RDD посредством чтения данных из csv-файла

Что будет выведено в результате выполнения кода?
val kvDF = Seq((1,2),(2,3)).toDF("key","value").show()
- DataFrame из двух колонок ("key","value").

К действиям над DataFrame относятся
- count, show(), first()

Что делает узелTaskTracker?
- главный узел организует общее выполнение задач MapReduce

Что происходит с RDD в момент вызова нового действия?
- вычисление всего набора с самого начала

За что отвечает следующий фрагмент кода:
data = my_inner_RDD.collect()
- получение элементов RDD в виде списка

Преобразование orderBy производит с DataFrame следующее
- Сортирует данные указанных столбцов, по умолчанию сортировка идет по возрастанию

count производит с DataFrame следующее...
- Возвращает количество строк в DataFrame

Выдели действия, применяющиеся к набору RDD? - reduce (func), countByValue 

Secondary NameNode это? - вспомогательный узел, который занимается мониторингом состояния кластера HDFS

Узел DataNode - осуществляет считывание и запись блоков HDFS в физические файлы и периодически передают NameNode информацию о состоянии

Операция groupBy, применяемая к DataFrame - это? - это трансформация группирует DataFrame по одному или нескольким столбцам, создавая новый DataFrame

Под какой из перечисленных узлов всегда выделяют отдельный сервер? - DataNode -- неверно 

Как выполняются преобразования наборов RDD? - поэлементно

Преобразование where производит с DataFrame следующее... - Фильтрацию строк на основе заданных логических условий, возвращая новый DataFrame

Выдели преобразования, применяющиеся к набору RDD? - reduceByKey(func), mapValues(func)

Что такое RDD? - неизменяемая коллекция данных 

DataFrame это? - распределенный набор данных, имеющий структуру, определяется схемой

Что служит результатом применения действий к RDD? - конкретное значение

Какой метод используется для ручного создания набора RDD? - parallelize()

За что отвечает следующий фрагмент кода:
new_rdd = my_inner_RDD.map(lambda x:x+2) -  получение нового набора RDD путем увеличения каждого элемента изначального RDD на 2

Выдели способа создания DataFrame? 1. Создайте через источник данных Spark: spark.read. ~ ("Путь"); 2. Конвертировано из RDD; 3. Запрос и возврат из HiveTable

Какая коллекция работает только со структурированными данными? - DataSet

Преобразование select  производит с DataFrame следующее... - Выбирает один или несколько столбцов из существующего набора столбцов в  DataFrame
